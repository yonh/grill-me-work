name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: "--target universal-apple-darwin"
          - platform: ubuntu-22.04
            args: ""
          - platform: windows-latest
            args: ""
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev \
            libayatana-appindicator3-dev

      - name: Install frontend dependencies
        run: npm ci

      - name: Build & Release (tauri-action)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Signing (optional): APPLE_CERTIFICATE(+_PASSWORD), APPLE_SIGNING_IDENTITY,
          # APPLE_ID / APPLE_PASSWORD, WINDOWS_CERTIFICATE(+_PASSWORD),
          # TAURI_SIGNING_PRIVATE_KEY(+_PASSWORD) for updater artifacts.
        with:
          tagName: v__VERSION__
          releaseName: "Grill-Me V2 v__VERSION__"
          releaseBody: |
            Grill-Me V2 v__VERSION__

            ## Install
            - **macOS** (Universal): `Grill-Me V2_*_universal.dmg`
            - **Windows**: `Grill-Me V2_*.msi` / `*.exe`
            - **Linux**: `*.deb` / `*.AppImage`
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
